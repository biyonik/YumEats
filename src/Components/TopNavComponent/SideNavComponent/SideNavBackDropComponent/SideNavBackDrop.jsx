export const SideNavBackDrop = ({sideNav}) => {
    return (
        sideNav ? (
            <div className="bg-black/60 fixed w-full h-screen z-10 top-0 left-0">

            </div>
        ): ("")
    )
}
